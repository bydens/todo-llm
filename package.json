{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Todo application",
  "main": "script.js",
  "scripts": {
    "build": "terser script.js -o script.min.js --compress --mangle && cleancss -o styles.min.css styles.css"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.2",
    "terser": "^5.28.1"
  }
}